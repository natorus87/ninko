apiVersion: v1
kind: Service
metadata:
  name: chromadb
  labels:
    app.kubernetes.io/name: chromadb
    app.kubernetes.io/part-of: ninko
spec:
  selector:
    app: chromadb
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
