apiVersion: v1
kind: Service
metadata:
  name: ninko-backend
  labels:
    app.kubernetes.io/name: ninko-backend
    app.kubernetes.io/part-of: ninko
spec:
  selector:
    app: ninko-backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
