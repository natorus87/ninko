apiVersion: v1
kind: Secret
metadata:
  name: ninko-conbro-local-tls
  namespace: ninko
type: kubernetes.io/tls
data:
  # base64 -w0 < tls.crt
  tls.crt: REPLACE_WITH_BASE64_CERT
  # base64 -w0 < tls.key
  tls.key: REPLACE_WITH_BASE64_KEY
