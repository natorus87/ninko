apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kumio

resources:
  - namespace.yaml
  # - ollama/   # kein Ollama in K8s – Embeddings laufen via LM Studio
  - chroma/
  - redis/
  # - vault/   # ersetzt durch SQLite-Backend (persistent via PVC)
  - backend/
  - searxng/
