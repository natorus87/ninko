{{- if .Values.searxng.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ninko.searxng.fullname" . }}
  namespace: {{ include "ninko.namespace" . }}
  labels:
    {{- include "ninko.labels" . | nindent 4 }}
    app.kubernetes.io/component: searxng
spec:
  type: ClusterIP
  selector:
    {{- include "ninko.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: searxng
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
{{- end }}
